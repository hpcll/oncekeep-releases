# Oncekeep 第一位用户 Mac 验收记录

本表必须从最终 HTTPS 一行安装入口开始执行；该入口下载的内容必须是最终 zip。测试机不得有源码 checkout，不得把开发机的 `app/`、`node_modules` 或安装根直接复制过去。双击 zip 路径作为离线安装和故障恢复另测。失败项保留现场并记录证据，不要用手工修改配置绕过后再标通过。

## 0. 测试身份

| 字段 | 记录 |
|---|---|
| 测试日期/测试人 |  |
| Mac 型号/芯片 |  |
| macOS 版本/Build |  |
| 新建或既有 macOS 用户 |  |
| 发行文件名 |  |
| zip SHA-256 |  |
| `installer-manifest.json` 版本/工件数 |  |
| 安装前是否存在 `~/.local/share/localbrain` |  |
| 选用 Vault 路径 |  |
| 是否位于 iCloud |  |
| 已安装 Agent 与版本 |  |

验收结果只用 `PASS`、`FAIL`、`BLOCKED`、`NOT RUN`。每个 `PASS` 必须有输出、截图、文件 hash 或可重复的唯一标记。

## 1. 下载和解压

| ID | 操作与判据 | 结果 | 耗时 | 证据/备注 |
|---|---|---|---|---|
| M-00 | 在干净终端只粘贴 README 的一行命令；不安装 Node/Python、不克隆源码、不使用 sudo |  |  |  |
| M-01 | 从交付渠道下载 zip；本机计算值与 `.sha256` 一致 |  |  |  |
| M-02 | 一行脚本自动下载、校验、临时解压并在结束后清理；离线检查包内有四个入口、README 和 bundle |  |  |  |
| M-03 | 包内无个人 Vault、日志、模型缓存、凭据或开发机绝对路径 |  |  |  |
| M-04 | 记录 Gatekeeper 首次打开的全部提示和需要的点击 |  |  |  |

## 2. 全新安装和首次启动

| ID | 操作与判据 | 结果 | 耗时 | 证据/备注 |
|---|---|---|---|---|
| M-10 | 一行命令完成安装；不再要求用户输入第二条命令或手动定位解压目录 |  |  |  |
| M-11 | beta.5 新安装默认 iCloud Drive/Oncekeep；账户/目录不可用时明确停止，不悄悄退回本地，不创建假的 CloudDocs；显式 local/custom 路径另测 |  |  |  |
| M-11A | 明确区分本地写入和云端上传；在 Finder / 另一台设备独立确认同步完成，不能仅凭本机文件存在标 PASS |  |  |  |
| M-11B | 既有本地或自定义 Vault 升级后原路径/身份不变，不创建默认 iCloud Vault、不自动搬迁 |  |  |  |
| M-12 | 安装完成，无 Node/npm/源码依赖报错 |  |  |  |
| M-13 | `~/Library/LaunchAgents/com.localbrain.daemon.plist` 存在且 root 正确 |  |  |  |
| M-14 | 自动打开 `/admin/setup` 并成功换取会话，不显示未认证错误 |  |  |  |
| M-15 | 后台显示的 root、Vault、端口与实际安装一致 |  |  |  |
| M-16 | 首次安装到可打开后台总耗时；目标 ≤10 分钟 |  |  |  |

## 3. 服务与重启

| ID | 操作与判据 | 结果 | 耗时 | 证据/备注 |
|---|---|---|---|---|
| M-20 | `/health` 返回成功，且只监听 loopback |  |  |  |
| M-21 | 注销并重新登录 macOS，服务自动启动 |  |  |  |
| M-22 | 重启整台 Mac 后，服务自动启动且只有一个 daemon |  |  |  |
| M-23 | 运行 `~/.local/share/localbrain/app/bin/oncekeep ui` 可重新进入后台，不依赖临时解压目录；双击入口另作备用检查 |  |  |  |
| M-24 | 日志没有快速重启循环或无界增长 |  |  |  |

## 4. Agent 发现、接入和验证

分别记录 Codex 和另一款真实 Agent；不存在的 Agent 必须显示 absent，不能创建其配置目录。

| ID | 操作与判据 | 结果 | Agent/版本 | 证据/备注 |
|---|---|---|---|---|
| M-30 | 首次设置页自动扫描，结果与机器实际安装一致，无需用户先点“重新扫描” |  |  |  |
| M-30A | 默认勾选“接入未来对话”和“导入本机历史”；用户可以单独取消历史导入 |  |  |  |
| M-31 | 一次确认前同时显示将修改的文件/键/备份位置，以及历史文件数、对话数、重复数和警告 |  |  |  |
| M-31A | 确认后只读取所选 Agent 的本地历史，导入结果列出 imported/duplicates/skipped/failed；重复执行不产生重复 turn |  |  |  |
| M-32 | 同一次确认完成 Agent 接入和历史导入，宿主要求的信任/重启提示准确；新对话随后自动捕获 |  |  |  |
| M-33 | MCP 握手与 `search/open` 实际通过 |  |  |  |
| M-34 | Agent A 对话被捕获；Vault 中有可追溯记录 |  |  |  |
| M-35 | Agent B 搜到 A 的唯一标记和正确 Obsidian 相对路径 |  |  |  |
| M-36 | Agent B 新对话可被 A 召回（若宿主能力允许双向） |  |  |  |
| M-37 | 重复一次相同会话不会产生重复 turn |  |  |  |

唯一标记格式：`ONCEKEEP-FIRSTUSER-YYYYMMDD-<随机8位>`。记录原始提问、最终回答、turn ID 和命中路径；不要在公开 issue 中粘贴真实私人正文。

## 4A. 两台 Mac 同 Vault 写入

先等待第一台 Mac 的 Vault 已完整同步到 iCloud，再在第二台 Mac 运行正常的一行安装，并通过 `ONCEKEEP_VAULT` 选择同一路径。不得复制第一台的 `config`、`state`、SQLite 或整个安装根。

| ID | 操作与判据 | 结果 | 耗时 | 证据/备注 |
|---|---|---|---|---|
| M-38 | 两台 Mac 的 `.localbrain/workspace.json` 使用同一 `vaultId`，设备登记文件和本机 config 使用不同 `deviceId` |  |  |  |
| M-39 | 两台 daemon 同时运行，各自在 `.localbrain/events/<deviceId>/` 写入，没有冲突副本 |  |  |  |
| M-39A | 两台用相同 Agent/session/turn key 各完成一轮，turn ID 和自动 Markdown 路径均不同 |  |  |  |
| M-39B | Mac A 写入唯一标记后 Mac B 可检索；Mac B 写入另一标记后 Mac A 可检索 |  |  |  |
| M-39C | Mac B 遗忘 Mac A 的测试回合后，两边均不再召回；Mac A 再次上线后其设备日志中旧正文被清理 |  |  |  |
| M-39D | 同时手工编辑同一个普通 Obsidian 文件的冲突按 iCloud/Obsidian 外部边界记录，不把冲突副本当作 Oncekeep 自动投影覆盖 |  |  |  |

## 5. 模型和降级

| ID | 操作与判据 | 结果 | 耗时 | 证据/备注 |
|---|---|---|---|---|
| M-40 | 无模型时仍可保存和浏览；明确显示语义检索与新分块索引不可用，不把有限关键词检索当完整检索 |  |  |  |
| M-41 | 本地模型下载显示进度、版本、大小和失败原因 |  |  |  |
| M-42 | 下载/预热成功后状态为 ready，语义检索实际参与 |  |  |  |
| M-43 | 断网或下载失败不会阻断 daemon 和保存/浏览；失败可重试，已校验模型不重复下载，重启后的中断状态可见 |  |  |  |

## 6. 备份、恢复和迁移

| ID | 操作与判据 | 结果 | 耗时 | 证据/备注 |
|---|---|---|---|---|
| M-50 | 创建备份并下载；清单/hash 校验通过 |  |  |  |
| M-51 | 恢复预览显示将新增、覆盖、冲突和跳过的内容 |  |  |  |
| M-52 | 恢复到全新隔离 Vault，唯一标记可召回 |  |  |  |
| M-53 | 原 Vault 在隔离恢复过程中未被修改 |  |  |  |

## 7. 升级和回滚

| ID | 操作与判据 | 结果 | 耗时 | 证据/备注 |
|---|---|---|---|---|
| M-60 | 已有安装再次运行新版固定 HTTPS 一行命令，识别为升级；双击入口另作备用检查 |  |  |  |
| M-61 | 升级前自动备份 core/config/受管 Agent 配置 |  |  |  |
| M-62 | 成功升级保留 Vault、身份、launcher key 和用户追加配置 |  |  |  |
| M-63 | 用故意损坏候选触发失败；旧版本自动恢复并可继续检索 |  |  |  |
| M-64 | 外部编辑的受管文件被保留并显示 partial/conflict |  |  |  |

## 8. 卸载和重装

| ID | 操作与判据 | 结果 | 耗时 | 证据/备注 |
|---|---|---|---|---|
| M-70 | 双击卸载入口，必须输入明确确认词 |  |  |  |
| M-71 | 程序和 LaunchAgent 被移除，服务停止 |  |  |  |
| M-72 | Vault、config/state/backups 保留；记录卸载前后 hash |  |  |  |
| M-73 | 重装后可选择保留数据恢复；若当前行为拒绝非空 root，明确记录产品缺口 |  |  |  |

## 9. 隐私和支持

| ID | 操作与判据 | 结果 | 耗时 | 证据/备注 |
|---|---|---|---|---|
| M-80 | 服务没有监听非 loopback 地址 |  |  |  |
| M-81 | 浏览器 URL/history 中不残留 bootstrap token |  |  |  |
| M-82 | 管理 API、日志和导出中不泄露 launcher key/Cookie/token |  |  |  |
| M-83 | 支持诊断包不含记忆正文、凭据、私人绝对路径和 Vault 文件 |  |  |  |
| M-84 | 用户能找到版本、安装根、Vault、服务状态和故障建议 |  |  |  |

## 10. 七天稳定性

每天记录 daemon 启动次数、Vault 新增文件数、重复 turn、pending/jobs/spool、日志大小、备份结果和一次唯一标记召回。

| 天 | daemon/重启 | 写入/重复 | pending/jobs/spool | 日志大小 | 备份 | 召回 | 结果/备注 |
|---|---|---|---|---|---|---|---|
| D1 |  |  |  |  |  |  |  |
| D2 |  |  |  |  |  |  |  |
| D3 |  |  |  |  |  |  |  |
| D4 |  |  |  |  |  |  |  |
| D5 |  |  |  |  |  |  |  |
| D6 |  |  |  |  |  |  |  |
| D7 |  |  |  |  |  |  |  |

## 结论

| 门槛 | 结果 | 证据/未通过原因 |
|---|---|---|
| 第一条记忆 ≤10 分钟 |  |  |
| Codex + 第二 Agent 跨 Agent 召回 |  |  |
| 两台 Mac 同 Vault 双向写入和召回 |  |  |
| 重启后服务恢复 |  |  |
| 升级失败回滚 |  |  |
| 备份/隔离恢复 |  |  |
| 卸载保留 Vault |  |  |
| 诊断包隐私检查 |  |  |
| 七天无数据丢失/重复/堆积/无界增长 |  |  |

最终判定：`ACCEPTED / REJECTED / BLOCKED`：

签字/日期：
